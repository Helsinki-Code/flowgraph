---
import { UserButton } from "@clerk/astro/components";

interface Props {
  title: string;
  description?: string;
}

const { title, description } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title} — Token Flame Graph</title>
    <meta name="description" content={description || "AI agent session profiler"} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@400;500;600&family=Syne:wght@700;800&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet" />
    <script>
      // Prevent flash of expanded sidebar on page load
      if (localStorage.getItem('sidebar-collapsed') === 'true') {
        document.documentElement.classList.add('sidebar-is-collapsed');
      }
    </script>
    <style>
      :root {
        --sidebar-width: 260px;
        --sidebar-collapsed-width: 68px;
        --accent-color: #f97316;
        --accent-hover: #ea580c;
        --bg-base: #09090d;
        --bg-sidebar: #0d0d12;
        --border-color: #1a1a24;
        --text-primary: #e8e8f0;
        --text-secondary: #888899;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(8px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      body {
        font-family: "IBM Plex Sans", -apple-system, sans-serif;
        background: var(--bg-base);
        color: var(--text-primary);
        line-height: 1.6;
      }

      h1, h2, h3, h4, h5, h6 {
        font-family: "Syne", -apple-system, sans-serif;
        font-weight: 700;
      }

      code {
        font-family: "JetBrains Mono", monospace;
      }

      .sidebar {
        position: fixed;
        top: 0;
        left: 0;
        width: var(--sidebar-width);
        height: 100vh;
        background: var(--bg-sidebar);
        border-right: 1px solid var(--border-color);
        border-top: 3px solid var(--accent-color);
        overflow-y: auto;
        padding: 1.5rem 1rem;
        display: flex;
        flex-direction: column;
        transition: width 240ms cubic-bezier(0.4, 0, 0.2, 1);
        z-index: 100;
      }

      body.sidebar-is-collapsed .sidebar {
        width: var(--sidebar-collapsed-width);
      }

      .logo {
        font-size: 1.25rem;
        font-weight: 800;
        margin-bottom: 0.5rem;
        display: flex;
        align-items: center;
        gap: 0.75rem;
        transition: opacity 180ms;
        min-height: 2.5rem;
      }

      .logo-icon {
        width: 1.5rem;
        height: 1.5rem;
        flex-shrink: 0;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .logo-text, .logo-tagline {
        transition: opacity 180ms, width 180ms;
        white-space: nowrap;
        overflow: hidden;
      }

      body.sidebar-is-collapsed .logo-text,
      body.sidebar-is-collapsed .logo-tagline {
        opacity: 0;
        width: 0;
      }

      .logo-tagline {
        font-size: 0.65rem;
        color: var(--text-secondary);
        font-weight: 400;
        font-family: "IBM Plex Sans", sans-serif;
        margin-top: -0.25rem;
      }

      .sidebar-toggle {
        position: absolute;
        right: 1rem;
        top: 1.5rem;
        background: transparent;
        border: none;
        color: var(--accent-color);
        cursor: pointer;
        font-size: 1.25rem;
        padding: 0.5rem;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: color 0.2s;
        z-index: 101;
      }

      .sidebar-toggle:hover {
        color: var(--accent-hover);
      }

      .nav {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
        margin-top: 2rem;
        flex: 1;
      }

      .nav a {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        padding: 0.75rem 1rem;
        border-radius: 0.375rem;
        color: var(--text-secondary);
        text-decoration: none;
        transition: all 0.2s;
        border-left: 3px solid transparent;
        font-size: 0.95rem;
      }

      .nav a:hover {
        background: rgba(249, 115, 22, 0.1);
        color: #b0b0c0;
      }

      .nav a.active {
        color: var(--accent-color);
        border-left-color: var(--accent-color);
        background: rgba(249, 115, 22, 0.12);
      }

      .nav-icon {
        width: 1.25rem;
        height: 1.25rem;
        flex-shrink: 0;
      }

      .nav-label {
        transition: opacity 180ms, width 180ms;
        white-space: nowrap;
        overflow: hidden;
      }

      body.sidebar-is-collapsed .nav-label {
        opacity: 0;
        width: 0;
      }

      .main {
        margin-left: var(--sidebar-width);
        min-height: 100vh;
        padding: 2rem 2.5rem;
        transition: margin-left 240ms cubic-bezier(0.4, 0, 0.2, 1);
        animation: fadeIn 0.3s ease-out;
      }

      body.sidebar-is-collapsed .main {
        margin-left: var(--sidebar-collapsed-width);
      }

      .header {
        margin-bottom: 2rem;
      }

      .header h1 {
        font-size: 2rem;
        font-weight: 800;
        margin-bottom: 0.5rem;
        color: var(--text-primary);
      }

      .header p {
        color: var(--text-secondary);
        font-size: 0.95rem;
        font-weight: 400;
      }

      .sidebar-bottom {
        margin-top: auto;
        padding-top: 1rem;
        border-top: 1px solid var(--border-color);
        display: flex;
        align-items: center;
        justify-content: center;
      }

      @media (max-width: 768px) {
        .sidebar {
          position: relative;
          width: 100%;
          height: auto;
          border-right: none;
          border-bottom: 1px solid var(--border-color);
          border-top: none;
          padding: 1rem 1.5rem;
          flex-direction: row;
          align-items: center;
          justify-content: space-between;
          margin-bottom: 0;
        }

        .sidebar.collapsed,
        body.sidebar-is-collapsed .sidebar {
          width: 100%;
        }

        .sidebar-toggle {
          display: none;
        }

        .main {
          margin-left: 0;
          padding: 1.5rem;
        }

        .nav {
          flex-direction: row;
          gap: 0.25rem;
          margin-top: 0;
          flex: none;
          overflow-x: auto;
        }

        .nav a {
          padding: 0.5rem 0.75rem;
          font-size: 0.85rem;
          border-left: none;
          border-bottom: 3px solid transparent;
        }

        .nav a.active {
          border-left: none;
          border-bottom-color: var(--accent-color);
        }

        .logo-text, .logo-tagline {
          opacity: 1;
          width: auto;
        }

        .nav-label {
          opacity: 1;
          width: auto;
        }
      }
    </style>
  </head>
  <body>
    <div class="sidebar">
      <button class="sidebar-toggle" id="sidebar-toggle" aria-label="Toggle sidebar">
        ‹
      </button>
      <div class="logo">
        <div class="logo-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="color: #f97316">
            <path d="M12 2C6.48 2 2 6.48 2 12c0 5.52 4.48 10 10 10s10-4.48 10-10S17.52 2 12 2zm0 18c-4.42 0-8-3.58-8-8s3.58-8 8-8 8 3.58 8 8-3.58 8-8 8zm3.5-9c.83 0 1.5-.67 1.5-1.5S16.33 8 15.5 8 14 8.67 14 9.5s.67 1.5 1.5 1.5zm-7 0c.83 0 1.5-.67 1.5-1.5S9.33 8 8.5 8 7 8.67 7 9.5 7.67 11 8.5 11zm3.5 6.5c2.33 0 4.31-1.46 5.11-3.5H6.89c.8 2.04 2.78 3.5 5.11 3.5z" fill="currentColor"/>
          </svg>
        </div>
        <div>
          <div class="logo-text">Flame Graph</div>
          <div class="logo-tagline">Token Observability</div>
        </div>
      </div>
      <nav class="nav">
        <a href="/app" class={Astro.url.pathname.startsWith("/app") && !Astro.url.pathname.includes("/sessions") && !Astro.url.pathname.includes("/settings") ? "active" : ""}>
          <svg class="nav-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <rect x="3" y="3" width="7" height="7"></rect>
            <rect x="14" y="3" width="7" height="7"></rect>
            <rect x="14" y="14" width="7" height="7"></rect>
            <rect x="3" y="14" width="7" height="7"></rect>
          </svg>
          <span class="nav-label">Sessions</span>
        </a>
        <a href="/app/cost" class={Astro.url.pathname === "/app/cost" ? "active" : ""}>
          <svg class="nav-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="12" r="8"></circle>
            <path d="M12 6v12M8 10h8"></path>
          </svg>
          <span class="nav-label">Cost Analysis</span>
        </a>
        <a href="/app/alerts" class={Astro.url.pathname === "/app/alerts" ? "active" : ""}>
          <svg class="nav-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"></path>
            <path d="M13.73 21a2 2 0 0 1-3.46 0"></path>
          </svg>
          <span class="nav-label">Alerts</span>
        </a>
        <a href="/app/billing" class={Astro.url.pathname === "/app/billing" ? "active" : ""}>
          <svg class="nav-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <rect x="2" y="7" width="20" height="14" rx="2" ry="2"></rect>
            <path d="M16 21V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v16"></path>
          </svg>
          <span class="nav-label">Billing</span>
        </a>
        <a href="/app/settings" class={Astro.url.pathname === "/app/settings" ? "active" : ""}>
          <svg class="nav-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="12" r="3"></circle>
            <path d="M12 1v6m0 6v6M4.22 4.22l4.24 4.24m3.08 3.08l4.24 4.24M1 12h6m6 0h6m-16.78 7.78l4.24-4.24m3.08-3.08l4.24-4.24"></path>
          </svg>
          <span class="nav-label">Settings</span>
        </a>
      </nav>
      <div class="sidebar-bottom">
        <UserButton />
      </div>
    </div>

    <main class="main">
      <div class="header">
        <h1>{title}</h1>
        {description && <p>{description}</p>}
      </div>

      <slot />
    </main>

    <script>
      const sidebar = document.querySelector('.sidebar');
      const toggle = document.getElementById('sidebar-toggle');
      const STORAGE_KEY = 'sidebar-collapsed';

      // Restore saved state
      if (localStorage.getItem(STORAGE_KEY) === 'true') {
        document.body.classList.add('sidebar-is-collapsed');
        toggle.textContent = '›';
      }

      toggle.addEventListener('click', () => {
        const isCollapsed = document.body.classList.toggle('sidebar-is-collapsed');
        localStorage.setItem(STORAGE_KEY, isCollapsed);
        toggle.textContent = isCollapsed ? '›' : '‹';
      });
    </script>
  </body>
</html>
